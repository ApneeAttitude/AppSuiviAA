# ToDo-List — Projet Apnée

Dernière mise à jour : 23/09/2026

## Accès et partage — dossier Drive "App Suivi"

- [ ] Revoir les droits d'accès des répertoires Drive des lignes L1, L2, L3, L4, LC, DNF, STA (qui a accès à chaque dossier, en lecture ou en édition)

## Application de suivi des présences (AppSuiviAA)

- [ ] Mettre à jour manuellement les classeurs PROD déjà déployés (L1-STA) suite à ce renommage : en-tête de la colonne F de l'onglet Presences (« Qualité de réalisation » → « Zone de confort ») et liste de validation associée (nouvelles valeurs Dans la zone / En limite / Hors zone) — la génération Python ne touche pas les classeurs déjà créés
- [x] Étendre cette analyse d'écarts Craft.me / Inscriptions aux autres lignes (L1, L3, L4, DNF, STA, groupe compétition), non encore comparées — **clos le 23/09/2026 sur décision de Fred** : pas d'audit systématique des lignes restantes, les écarts seront remontés au fil de l'eau s'il y en a.
- [ ] Tester quelques cas unitaires de la synchronisation (cas simples ciblés) avant de passer à un cas réel complet
- [ ] Une fois le développement du fichier de paramétrage centralisé terminé : tester la synchronisation sur l'exemple réel du document Craft.me ci-dessus (composition des groupes), après les cas unitaires
- [ ] Une fois validé : ajouter un menu "Synchroniser" sur le classeur de Paramétrage + un déclencheur `onOpen` (et un filet de sécurité en tâche planifiée nocturne), pour ne plus avoir à lancer la synchronisation depuis l'éditeur Apps Script
- [ ] Porter aussi la synchronisation du Référentiel (Objectifs, Compétences, Créneaux, Responsables, Qualifications Sécurité) et l'initialisation du Calendrier — `sync_referentiel.py` le fait déjà, mais la mise en page (positions figées) des classeurs PROD actuellement déployés n'a pas encore été vérifiée ligne à ligne comme elle l'a été pour Personnes
- [x] Créer et configurer les nouvelles lignes de suivi **DNF1, DNF2, STA1 et STA2** (STA3 abandonné, sur décision de Fred le 23/09/2026 — n'a jamais été créé) : classeur Google Sheet, dossier Drive et droits d’accès, cible Apps Script, puis page AppSuiviAA correspondante après validation en TEST. **Préparation du modèle DNF réalisée le 13/09/2026 :** une ligne de séances est désormais distincte d'un groupe de niveau. Les générateurs lisent `Lignes_Seances` (capacité et période d'effet), `Regles_Acces_Seances` (groupes autorisés et période), et `Creneaux_Affectations` (créneau et encadrants). La synchronisation construit le roster DNF à partir des groupes autorisés et initialise le calendrier dans la période de vie de la ligne. Le modèle de suivi a été porté à 150 personnes : DNF1 compte actuellement 110 inscriptions actives éligibles. La colonne `Encadrant référent` a été ajoutée le 13/09/2026 à `Creneaux_Affectations` : **Yvan Measson** est référent DNF1, **Katarina Stankiewicz** est référente DNF2. Les deux modèles TEST ont été importés puis convertis en Google Sheets natives dans les dossiers Drive `DNF1` et `DNF2`. Les cibles Apps Script isolées `TEST-DNF1` et `TEST-DNF2` sont configurées le 13/09/2026. Après correction d’un premier déploiement resté à tort sur la version 42 (« cible inconnue »), la version TEST 45 apporte la migration des deux classeurs : `Statut (ID)` (1=planifiée, 2=tenue, 3=annulée, 4=fermée) et Zone de confort normalisée (1=Confort, 2=Challenge, 3=Limite, avec libellé lisible dans Presences). Les pages GitHub Pages `_test/DNF1` et `_test/DNF2` utilisent ces listes et portent désormais explicitement le titre DNF1/DNF2. Validation technique confirmée (DNF1 : Yvan Measson, DNF2 : Katarina Stankiewicz) et validation fonctionnelle par un encadrant encore attendue avant toute livraison en PROD. **Préparation PROD des données réalisée le 13/09/2026 :** les copies natives `AA - Suivi DNF1 2026-2027` et `AA - Suivi DNF2 2026-2027` ont été créées dans leurs dossiers Drive respectifs, avec les mêmes 24 personnes ayant accès. Les cibles Apps Script `PROD-DNF1` et `PROD-DNF2` sont maintenant reliées à ces seuls classeurs et les migrations manuelles `preparerDnf1Prod` / `preparerDnf2Prod` ont abouti : Zone de confort normalisée (1=Confort, 2=Challenge, 3=Limite, avec libellé lisible) et Statut de séance (1=planifiée, 2=tenue, 3=annulée, 4=fermée). Les tables ont été contrôlées dans les deux classeurs. Les pages `DNF1/index.html` et `DNF2/index.html` ont été publiées sur GitHub Pages le 13/09/2026 (commit `3f6b8d1`) et le déploiement Apps Script PROD a été promu en version 46. Validation après publication : les deux pages répondent, et les appels API PROD renvoient les séances, encadrants référents, zones de confort et statuts attendus (DNF1 : Yvan Measson ; DNF2 : Katarina Stankiewicz).
- [ ] **Préparation STA1 / STA2 (14/09/2026)** : dans la Google Sheet centrale, création du créneau `CR-VEN-25` (vendredi 21h30–22h30, bassin de 25 m, une ligne), des deux lignes de séance et de leurs règles d’accès : STA1 est ouverte à L1/L2, STA2 à L3/L4. Les affectations désignent Victorine Chardonnet et François Memheld pour STA1, Guillaume Boulant pour STA2. Une colonne `Périodicité (jours)` a été ajoutée à `Creneaux_Affectations`, avant `Contrôle`, et porte la valeur 14 pour chaque affectation : STA1 démarre le 18/09/2026, STA2 le 25/09/2026. Les contrôles de la table sont au vert. Les générateurs interprètent désormais une cellule vide comme 7 jours, respectent une périodicité de 14 jours et ne dupliquent pas les séances lorsqu’une ligne a plusieurs encadrants. Les deux classeurs locaux ont été générés et recalculés : STA1 contient 21 séances du 18/09/2026 au 25/06/2027 ; STA2 en contient 20 du 25/09/2026 au 18/06/2027. Les copies Google Sheets natives de TEST sont créées dans leurs dossiers Drive : STA1 (`1QGzitEk7jM7undte_y_EmaQYhsK_faKcFHTyni9folo`) et STA2 (`1PwMAxWjeOEE8Qpe0DRNLrVwv-KqmHZMlsTqFKloqBPg`). Les quatre premières séances de chaque calendrier ont été vérifiées dans Google Sheets : alternance correcte, aucun doublon. Les cibles isolées `TEST-STA1` et `TEST-STA2` ont été ajoutées à Apps Script ; les migrations `preparerSta1Test` et `preparerSta2Test`, strictement limitées à ces deux cibles, ont créé les listes normalisées de Zone de confort et Statut de séance. Le déploiement TEST est en version 47 (14/09/2026) ; PROD reste inchangé en version 46. Les appels API TEST sont concluants : STA1 expose la séance du 18/09/2026 avec François Memheld, STA2 celle du 25/09/2026 avec Guillaume Boulant, et les listes normalisées attendues. Les pages GitHub Pages `_test/STA1` et `_test/STA2` sont publiées et reliées au déploiement TEST (commit `c8cdda5`, 15/09/2026). **Validation fonctionnelle TEST confirmée par Fred le 15/09/2026.** **Livraison PROD autorisée et réalisée le 15/09/2026 :** deux copies natives distinctes `AA - Suivi STA1 2026-2027` et `AA - Suivi STA2 2026-2027` ont été créées dans leurs dossiers Drive, avec les mêmes 24 personnes ayant accès. Les cibles `PROD-STA1` et `PROD-STA2` sont reliées exclusivement à ces copies. Les migrations `preparerSta1Prod` et `preparerSta2Prod` ont créé les listes normalisées Zone de confort (1=Confort, 2=Challenge, 3=Limite) et Statut de séance (1=planifiée, 2=tenue, 3=annulée, 4=fermée). Apps Script PROD a été promu en version 48. Les appels API de contrôle renvoient STA1 (18/09/2026, François Memheld) et STA2 (25/09/2026, Guillaume Boulant), avec les listes attendues. Les pages `STA1/` et `STA2/` sont publiées sur GitHub Pages (commit `d87d178`) et leurs URLs ont été vérifiées.
- [ ] **P1 — Application à URL unique et menu hamburger :** première page TEST créée le 19/09/2026 sous `_test/index.html`. Elle propose les lignes de TEST réellement isolées (L2, DNF1, DNF2, STA1, STA2 et STAC), conserve la saisie existante et ouvre par défaut la séance du jour, sinon la prochaine. Le menu donne accès aux statistiques de la ligne, aux statistiques générales et au guide ; le thème retenu est **Océan profond**. La structure est alignée le 19/09/2026 sur la maquette de saisie directe et utilise la feuille commune `assets/appsuivi.css`. L1, L3, L4 et LC sont aussi visibles dans le menu, mais désactivées avec l’indication « Classeur TEST à créer » : elles ne doivent pas pointer vers une cible `PROD-*` pour un essai. La version PROD est publiée à l'URL unique `/?ligne=…` pour les dix lignes, dont STAC depuis le 20/09/2026 ; les anciennes URL restent disponibles pendant la transition. Les statistiques de ligne utilisent le même contrôle Google/ID/rôle sur PROD (Apps Script version 64) ; les encadrants DNF et STA sont aussi reconnus depuis `Creneaux_Affectations`. Reste à compléter les responsables autorisés aux statistiques globales, puis décider des raccourcis depuis les anciennes URL.
- [ ] Authentification : gérer des tokens pour ne pas la redemander à chaque fois
- [ ] Améliorer les temps de réponse au chargement
- [ ] Automatiser la chaîne CI/CD (déploiement du script Apps Script et publication GitHub Pages)
- [ ] Adapter les scripts du dossier `generateurs/` (`build_parametrage.py`, `build_suivi.py`, `sync_referentiel.py`, `check_classeurs.py`) : ils supposent une arborescence `generateurs/` + `classeurs/` (chemins relatifs du type `../classeurs/...`), alors qu'AppSuiviAA range chaque ligne dans son propre dossier (`L1/`, `L2/`, `L3/`, `L4/`, `LC/`, `DNF/`, `STA/`)
- [ ] Ajouter les fichiers sources de `generateurs/` au suivi Git puis les committer. Inclure les scripts Python et leurs données techniques nécessaires à la génération ; exclure les classeurs générés, exports et données opérationnelles du club.
- [ ] Supprimer les répertoires OneDrive inutiles : `mvp-secours-presences`, `App AA DEV`, `App AA MVP` (déjà supprimés deux fois par Fred mais réapparus — vérifier sur onedrive.com, dans la corbeille, et sur les autres appareils synchronisés avant de resupprimer)
- [ ] **Point de vigilance signalé à Fred, non traité ici** : `DNF` et `STA` ont chacun leur propre `WEB_APP_URL = "REMPLACER_PAR_URL_EXEC_PROD"` non renseigné dans le front-end, qui correspond à des `CLASSEURS['PROD-DNF']`/`['PROD-STA']` eux aussi non configurés (placeholders) dans `Code.gs`. C'est un défaut de configuration préexistant (probablement ces deux lignes n'ont jamais été mises en service réel) — pas lié aux déploiements du plan de séance ci-dessus, et volontairement laissé de côté pour ces tâches. À traiter séparément si Fred souhaite activer DNF/STA en PROD (même geste que pour TEST-L2 : créer/identifier le classeur Google Sheet, renseigner son ID dans `CLASSEURS`, renseigner `WEB_APP_URL`, redéployer).
- [ ] **Inventaire complet des listes de validation non dynamiques, sur demande de Fred (12/09/2026)** : suite au constat de Fred sur `Suivi_Competences!Lestage` (menu déroulant classique, liste codée en dur, plutôt qu'un menu déroulant depuis une plage sourcé sur l'onglet Listes), un diagnostic temporaire (`_diagScanValidations`, action `diagValidations`, retiré ensuite — Version 25 propre sur TEST) a scanné toutes les validations de données (`getDataValidations()`) de tout le classeur TEST-L2. **6 listes codées en dur trouvées, sur 19 colonnes au total** — chacune correspond à une des listes de référence déjà présentes dans l'onglet Listes, mais sans lien dynamique avec elle :
  1. `Calendrier!H` — Nature de séance (12 valeurs : ordinaire, technique, hypercapnique, mixte, hypoxique, test maximal, RIFAA, PSM, examen, formation, tutorat, multi-lignes)
  2. `Calendrier!I` — Statut de séance (4 valeurs : planifiée, tenue, annulée, fermée)
  3. `Suivi_Objectifs!S:Y` (7 colonnes, une par objectif de la ligne) — Statut d'acquisition (non tenté, non réussi, réussi)
  4. `Suivi_Competences!E:L` (8 colonnes, dont **E = Lestage**, la colonne signalée par Fred) — Niveau de compétence (non acquis, en cours, acquis, expert)
  5. `Evenements_Securite!H` — Type d'événement (7 valeurs : perte de contrôle moteur (PCM), samba, syncope, arrêt de séance, malaise, barotraumatisme, autre)
  6. `Evenements_Securite!I` — Gravité (3 valeurs : mineur, significatif, grave)

  Pour mémoire, hors périmètre de ce constat : la liste **Jours** (déjà pilotée par une formule native dans `Calendrier!C`, pas par une validation — voir la régression corrigée plus haut) et **Zone de confort** (déjà migrée vers le modèle ID+libellé sur TEST-L2, plus aucune validation native sur cette colonne : le menu est désormais reconstruit par l'appli elle-même) ne sont pas concernées par ce chantier. Toutes les colonnes de sélection d'apnéiste/séance/encadrant (`Selection!A2:A151`, `Calendrier!A5:A177`, `Referentiel!G54:G63`) sont déjà des validations « depuis une plage », donc déjà dynamiques — aucune ne référence toutefois directement l'onglet Listes (elles pointent vers Selection/Referentiel/Calendrier, ce qui est le comportement attendu pour ce type de colonne).
  **Conversion effectuée le 12/09/2026**, sur validation de Fred (« lance la conversion des 6 validations », « garde l'ordre des listes de l'onglet Liste ») : une fonction temporaire (`_convertirValidationsDepuisListes`, action `convertirValidations`, retirée ensuite — Version 27 propre sur TEST) a localisé automatiquement, pour chacune des 6 listes, le bloc correspondant dans l'onglet Listes (repéré par son titre en ligne 1, valeurs à partir de la ligne 3) et remplacé la validation codée en dur par une validation « depuis une plage » pointant directement dessus — l'ordre d'affichage du menu déroulant suit donc désormais l'ordre des valeurs dans Listes, comme demandé. Le critère « autoriser une valeur en dehors de la plage » (avertissement vs rejet strict) de chaque validation d'origine a été conservé à l'identique. Correspondance trouvée et appliquée pour les 19 colonnes, aucune liste introuvable :
  - `Calendrier!H5:H177` → `Listes!I3:I14` (Natures de séance, 12 valeurs)
  - `Calendrier!I5:I177` → `Listes!K3:K6` (Statuts de séance, 4 valeurs)
  - `Suivi_Objectifs!S8:Y107` (7 colonnes) → `Listes!E3:E5` (Statut d'acquisition, 3 valeurs)
  - `Suivi_Competences!E6:L105` (8 colonnes, dont Lestage) → `Listes!C3:C6` (Niveaux de compétence, 4 valeurs)
  - `Evenements_Securite!H5:H104` → `Listes!O3:O9` (Types d'événement, 7 valeurs)
  - `Evenements_Securite!I5:I104` → `Listes!Q3:Q5` (Gravités, 3 valeurs)

  Vérifié dans l'interface Sheets (Données > Validation des données) sur `Suivi_Competences!E6` : la règle affiche bien « Menu déroulant (depuis une plage) », source `=Listes!$C$3:$C$6` — plus une liste codée en dur. Les valeurs déjà saisies restent valides (mêmes libellés, seule la source du menu change). Scope strictement TEST-L2, comme le reste de ce chantier — à déployer plus largement seulement après validation de Fred sur ce premier cas.
- [x] Tester `synchroniserEffectifs` en manuel depuis l'éditeur Apps Script (menu Exécuter) et relire les onglets Personnes des classeurs PROD avant de faire confiance au résultat (Fred, après sa pause du 06/09/2026) — refait à plusieurs reprises depuis (dernière fois le 23/09/2026, pour refléter la suppression de Sébastien Dussault), résultats contrôlés à chaque fois.
- [x] **Harmoniser les libellés affichés des lignes (20/09/2026, TEST et PROD) :** `Ligne compétition` → `Compétition` ; `Intermédiaire` → `Intermédiaires` ; `DNF1` → `Apnée dynamique sans palme débutants` ; `DNF2` → `Apnée dynamique sans palme confirmés` ; `STA1` → `Apnée statique débutants` ; `STA2` → `Apnée statique confirmés` ; `STAC` → `Apnée statique compétition` (déjà correct). Ce libellé n'avait en réalité jamais été harmonisé depuis l'ajout des lignes DNF/STA : DNF1/DNF2 partageaient le même libellé (« Apnée dynamique sans palme »), tout comme STA1/STA2 — impossible de les distinguer dans le menu. Corrigé dans `_test/index.html` (`LIGNES_TEST`) et `index.html` (`LIGNES_PROD`, déjà en production).
  **Correction du 22/09/2026 :** une retouche ultérieure du 20/09 avait retiré « Apnée » de ces libellés, mais seulement sur TEST (voir tiroir latéral plus bas) — PROD était resté sur « Apnée dynamique sans palme débutants », etc. Les deux étaient donc en désaccord. Sur confirmation de Fred, PROD est aligné sur TEST (sans « Apnée ») : `Dynamique sans palme débutants/confirmés`, `Statique débutants/confirmés`, `Statique compétition`.
- [x] **Vérification complémentaire suite à la régression Jour/Calendrier (11/09/2026)**, sur demande de Fred (« Y a-t-il d'autres cas de formules de la sorte dans d'autres onglets ? ») : un nouveau diagnostic temporaire (`_diagScanTout`, action `diagTout`) a scanné l'ensemble des formules de tout le classeur TEST-L2 à la recherche de toute référence à `Listes!` — **résultat : aucune autre occurrence que celles déjà corrigées dans `Calendrier!C`** (173 cellules, C5:C177, un seul point de référence dans tout le classeur). Code de diagnostic retiré et déploiement TEST redéployé propre (Version 23) ; `Code.gs` redevient identique au commit `f91fa77`, aucun nouveau commit nécessaire pour ce fichier.
- [x] **Régression « Jour » dans Calendrier suite à la réorganisation de l'onglet Listes, corrigée le 10/09/2026** — signalée par Fred (captures d'écran du sélecteur de séance : le jour affiché ne correspondait plus au créneau, ex. une séance du lundi affichait « Valeur », une du mardi affichait « lundi », etc.). Cause : la colonne C (« Jour ») de l'onglet `Calendrier` est calculée par une formule native du classeur (pas par `Code.gs`) : `=INDEX(Listes!$A$2:$A$8;WEEKDAY($B5;2))`, qui supposait la liste des Jours sur `Listes!A2:A8` (7 valeurs, lundi→dimanche, juste après le titre, sans ligne d'en-tête) — la mise en page D'ORIGINE. Or la réorganisation de l'onglet Listes (voir entrée ci-dessus) a inséré une ligne d'en-tête « Valeur » en ligne 2 pour chaque liste, décalant les 7 valeurs réelles vers `A3:A9` : chaque `INDEX` renvoyait donc systématiquement la valeur de la ligne précédente (lundi affichait l'en-tête « Valeur », mardi affichait « lundi », etc.), exactement le symptôme observé. Fred a lui-même retrouvé la formule fautive et proposé un correctif — mention pour mémoire : la plage qu'il proposait (`Listes!$A$3:$A$3`, une seule cellule) était probablement une coquille, un `INDEX`/`WEEKDAY` ayant besoin des 7 lignes pour résoudre les 7 jours ; la plage appliquée est `Listes!$A$3:$A$9` (les 7 valeurs à leur nouvel emplacement).
  Diagnostic : plutôt que de supposer que seule cette formule référençait `Listes!A2:A8`, une fonction de diagnostic temporaire (`_diagScanListesRefs`, appelée via une action `diagListes` ajoutée temporairement à `doGet`) a scanné toutes les formules de tout le classeur contenant `Listes!` — un seul point de référence trouvé, `Calendrier!C`, avec 173 occurrences. Correctif appliqué par **Édition > Rechercher et remplacer** (« Rechercher également dans les formules » coché, toutes les feuilles) : `Listes!$A$2:$A$8` → `Listes!$A$3:$A$9`, confirmé par Sheets lui-même : « 173 occurrences remplacées » — count qui correspond exactement au diagnostic, confirmant que c'était bien le seul et unique endroit à corriger. Vérifié visuellement dans `Calendrier` (colonne Jour désormais cohérente avec chaque créneau) et par appel direct à l'API (`action=data&cible=TEST-L2` : `jour` correct pour chaque séance). Code de diagnostic temporaire retiré de `Code.gs` (fonction `_diagScanListesRefs` et bloc `action === 'diagListes'` dans `doGet`) et déploiement **TEST** redéployé propre (Version 20) — ce nettoyage ramène `Code.gs` à l'identique du contenu déjà commité (`f91fa77`), donc aucun nouveau commit nécessaire pour ce fichier. **Scope strictement TEST-L2**, comme le chantier Listes qui a introduit cette régression.
- [x] **Réorganisation de l'onglet Listes + colonne « Zone de confort (libellé) » dans Presences, sur TEST-L2 uniquement (10/09/2026)**, sur demande de Fred : deux changements liés. 1) Dans `Presences`, une colonne « Zone de confort (libellé) » est insérée juste après « Zone de confort (ID) » (Observation de l'encadrant / Contrôle décalées d'une colonne), avec une formule affichant le libellé résolu depuis l'ID — pour une lecture directe du Sheet, sans passer par l'appli. 2) Dans l'onglet `Listes`, chacune des 9 listes historiques (Jours, Niveaux de compétence, Statut d'acquisition, Qualité de réalisation, Natures de séance, Statuts de séance, Avis d'encadrant, Types d'événement, Gravités) plus la table Zone de confort sont désormais présentées en titre / ligne d'en-tête (« Valeur », ou « ID | Libellé | Actif » pour Zone de confort) / valeurs, séparées d'une colonne vide entre chaque liste — remplaçant les colonnes de libellés bruts sans en-tête. La table Zone de confort n'étant plus à un emplacement fixe (sa position dépend désormais du nombre de valeurs des listes précédentes), `Code.gs` (`lireListeZoneConfort_`) la repère par la plage nommée `ListeZoneConfort` plutôt que par coordonnées (`Listes!K:M` précédemment) — un classeur sans cette plage nommée (tous les PROD actuellement) n'est simplement pas migré vers ce modèle.
  Trois anomalies rencontrées et corrigées en cours de route, pour mémoire : **(a)** formules `#ERROR!` — le classeur est en locale `fr_FR`, et si `Range.setFormulas` écrit toujours en syntaxe US (virgules), le moteur de calcul de ce classeur exige des points-virgules pour évaluer `VLOOKUP`/`IFERROR` sans erreur ; formules réécrites avec `;`. **(b)** incident de corruption — la fonction de réorganisation (`reorganiserListesEtPresences`) suppose la mise en page D'ORIGINE de l'onglet Listes (colonnes 1-9 contiguës, pas de ligne d'en-tête) ; relancée par erreur une seconde fois sur le classeur déjà migré, elle a relu et corrompu les listes. Corrigé par restauration d'une version antérieure du classeur (historique des versions Google Sheets). Un garde-fou a été ajouté au code (erreur explicite si la plage `ListeZoneConfort` existe déjà), ainsi qu'une fonction séparée et rejouable sans risque, `corrigerFormulesLibellePresences(cible)`, qui ne régénère que les formules de la colonne libellé sans toucher à l'onglet Listes. **(c)** anomalie trouvée après redéploiement — la plage nommée `ListeZoneConfort` s'est retrouvée étendue à 6 lignes (`Listes!S3:U8`) au lieu de 3, avec les 3 valeurs attendues (Hors zone/En limite/Dans la zone) marquées « Actif=non » et 3 lignes en doublon (ID 1/2/3 réutilisés — contraire à la règle « un ID n'est jamais réutilisé » du document de proposition) portant des libellés parasites (Limite/Challenge/Confort) marquées « Actif=oui », reste probable de l'incident (b). Corrigé directement dans le Sheet : plage nommée redimensionnée à `Listes!S3:U5`, les 3 lignes légitimes repassées à « Actif=oui », les 3 lignes parasites supprimées.
  Redéployé sur le déploiement **TEST** (Version 17 du 10/09/2026). Vérifié par appel direct à l'API après correction : `action=data&cible=TEST-L2` renvoie exactement les 3 valeurs actives attendues (Hors zone/En limite/Dans la zone) ; `action=fiche&cible=TEST-L2&seance=...` renvoie `qualite`/`observation` correctement malgré le décalage de colonne dans Presences. **Scope strictement TEST-L2**, comme le premier volet de ce chantier — à déployer plus largement seulement après validation de Fred.
- [x] **Séparation des déploiements Apps Script TEST / PROD (10/09/2026)**, suite à la régression ci-dessus et à la demande explicite de Fred (« il faut complètement séparer les environnements de test et de prod ») : conservé **un seul fichier source `Code.gs`** (pas de duplication de code, donc pas de risque de divergence entre deux copies), mais désormais **deux déploiements Apps Script indépendants** du même projet. Le déploiement existant (ID `AKfycbxTeOKaPx53j2exXGt6Hql8BIe22WRzxTqCiY3gpe60gvRmj8BanUjIH9tsrnoCsPw94g`) reste le déploiement **PROD**, épinglé sur la Version 15 (le correctif de régression) — les 7 front-ends PROD (L1, L2, L3, L4, LC, DNF, STA) gardent cette URL sans aucune modification. Un nouveau déploiement **TEST** a été créé à partir du même projet (ID `AKfycbwkFxW3sBuR9ccboDcp5htVmrLWiIrcfvdi2O3UYtWK7blG7cxxFHHiCPdyZeMEbeyC2A`, Version 16, même code que la Version 15 au moment de la création), avec la même configuration (Exécuter en tant que Moi, Accès Tout le monde). `_test/L2/index.html` et `_test/L3/index.html` ont été mis à jour pour pointer vers cette nouvelle URL TEST (`WEB_APP_URL`) au lieu de l'URL partagée avec PROD. Vérifié par appel direct : `action=data&cible=TEST-L2` sur la nouvelle URL renvoie bien le roster TEST-L2 (200, `ok:true`). **Nouveau workflow à partir de maintenant** : toute évolution (comme le chantier Zone de confort) se déploie d'abord sur le déploiement TEST uniquement (Déployer → Gérer les déploiements → modifier le déploiement TEST → Nouvelle version) ; ce n'est qu'après validation par Fred sur TEST-L2/TEST-L3 qu'on « promeut » la même version en éditant le déploiement PROD pour y épingler cette version validée (Déployer → Gérer les déploiements → modifier le déploiement PROD → sélectionner cette version, jamais « Nouvelle version » à cette étape). Ce découplage évite qu'une validation ajoutée pour un besoin TEST ne s'applique par erreur à PROD avant d'avoir été explicitement promue.
- [x] **Régression PROD suite au chantier Zone de confort, corrigée le 10/09/2026** — signalée par Fred (« impression de régression sur la saisie de la zone de confort » sur PROD-L3). Cause : `Code.gs` est un script partagé par **tous** les environnements (une seule URL `/exec`) ; la validation V-02 ajoutée pour Zone de confort (cf. entrée ci-dessus) s'appliquait donc à tort sur tous les classeurs, alors que seul TEST-L2 a une table `Listes!K:M` renseignée. Sur PROD (et TEST-L3), cette table est vide → `zonesValides` était vide → **toute présence avec une zone de confort renseignée était rejetée** à l'enregistrement (« zone de confort inconnue du référentiel »). Mitigation immédiate : Fred a repassé le déploiement en Version 13 (avant ce chantier) le temps du correctif. Correctif définitif : la validation V-02 est désormais conditionnée à `zoneConfortMigree` (`Listes!K:M` non vide pour le classeur concerné) — sur un classeur non migré, la zone de confort reste un texte libre non validé, comme avant. Déployé en **Version 15** (même URL). Vérifié par exécution directe dans l'éditeur Apps Script : `lireListeZoneConfort_` renvoie `[]` pour PROD-L3 (`zoneConfortMigree` = false, validation désactivée) et les 3 valeurs attendues pour TEST-L2 (`zoneConfortMigree` = true, validation toujours active). **Leçon retenue** : sur ce script partagé, toute nouvelle validation doit être scopée par classeur (via une condition sur les données du classeur, pas sur le nom de la cible) avant d'être déployée — jamais supposer qu'un changement scopé « TEST-L2 seulement » dans le code front reste sans effet côté back partagé.
- [x] **Listes de valeurs ID + libellé : Zone de confort migrée, sur TEST-L2 uniquement (09/09/2026)**, suite à la validation par Fred de `claude/proposition-listes-de-valeurs.md`. Modèle implémenté tel que proposé (ID entier stable + libellé résolu, colonne « Actif » pour retirer une valeur sans casser les présences déjà enregistrées) : `Listes!K2:M4` porte la table de référence (1=Hors zone, 2=En limite, 3=Dans la zone, toutes actives) ; `Presences` colonne F stocke désormais l'ID (migré depuis le texte libre, F5:F9) au lieu du libellé, colonne F renommée « Zone de confort (ID) », et une nouvelle colonne I (« Zone de confort (libellé) », formule `=IFERROR(VLOOKUP($F5;Listes!$K$2:$L$4;2;FALSE);"")`) affiche le libellé pour une lecture directe du Sheet sans passer par l'appli. Trouvaille au passage : les puces du menu déroulant natif de la colonne F (fonctionnalité Tableau de Sheets) étaient en minuscules (« dans la zone ») alors que les données saisies étaient capitalisées (« Dans la zone ») — mismatch préexistant, non lié à ce chantier, source d'avertissements « Non valide » sur toutes les cellules F déjà remplies ; réglé de fait par le passage aux ID numériques. Côté `Code.gs` : nouvelle fonction `lireListeZoneConfort_` (lit `Listes!K:M`), ajoutée à la réponse `action=data` (`listesZoneConfort`), et `savePresences_` refuse désormais tout ID de zone de confort absent du référentiel (V-02, même principe que V-01 pour l'apnéiste). Côté `_test/L2/index.html` : le menu déroulant « Zone de confort » n'est plus codé en dur, il est reconstruit à partir de `listesZoneConfort` (seules les valeurs actives proposées pour une nouvelle saisie ; une valeur déjà sélectionnée mais retirée depuis reste visible) et la valeur envoyée à l'enregistrement est l'ID numérique. Déployé (Version 14, même URL `/exec`) et vérifié par appel direct à l'API : `action=data&cible=TEST-L2` renvoie la table de référence, `action=fiche&cible=TEST-L2&seance=...` renvoie les présences existantes avec `qualite` numérique (migration confirmée). **Scope strictement TEST-L2** : ni TEST-L3, ni aucune ligne PROD ne sont concernées à ce stade — à déployer plus largement seulement après validation de Fred sur ce premier cas. Onglet `Listes` colonne D (« Qualité de réalisation », valeurs obsolètes) volontairement laissée en l'état : rien ne la lit.
- [x] **Déploiement en PROD du titre de lien personnalisable (09/09/2026)**, après validation de Fred sur TEST-L2/TEST-L3 (« Ok ça fonctionne ») : même fonctionnalité appliquée aux 7 lignes de production **L1, L2, L3, L4, LC, DNF, STA**. Vérifié fichier par fichier (seules les différences attendues titre/en-tête/CIBLE/WEB_APP_URL DNF-STA subsistent) et JS validé avec `node --check` sur les 7 fichiers. Commit `2f0148d`.
- [x] **Titre de lien personnalisable dans le plan de séance, en test (09/09/2026)**, sur demande de Fred : dans `_test/L2` et `_test/L3` uniquement, le bouton « lien » de l'éditeur de texte riche demande maintenant un texte à afficher en plus de l'URL (quand rien n'est sélectionné dans l'éditeur — si du texte est déjà sélectionné, il sert toujours de titre comme avant). Le lien inséré est désormais `<a href="URL">Titre</a>` plutôt que d'afficher l'URL brute comme texte visible. Titre laissé vide → on retombe sur l'URL comme texte ; saisie annulée → aucun lien inséré. Commit `a190537`.
- [x] **Déploiement en PROD de l'éditeur de texte riche pour le plan de séance (08/09/2026)**, sur validation de Fred après tests concluants sur TEST-L2/TEST-L3 : la même fonctionnalité (barre d'outils Gras/Italique/Liste/Lien, sanitisation allowlist, hauteur auto-adaptative 110px→75vh) est désormais appliquée aux 7 lignes de production **L1, L2, L3, L4, LC, DNF, STA**. Vérifié fichier par fichier : seules les différences attendues subsistent entre les 7 `index.html` (titre, en-tête, `CIBLE`, et — pour DNF/STA uniquement — leur `WEB_APP_URL` déjà en placeholder, voir point ci-dessous) ; JS de chaque fichier validé avec `node --check`. Commit `96c774f`.
- [x] **Hauteur automatique du plan de séance (08/09/2026)**, suite au retour de Fred : la hauteur fixe à 75vh choisie initialement était trop importante. La zone de saisie grandit désormais avec le contenu (comme un textarea auto-resize), avec un minimum de 110px et un plafond conservé à 75vh (au-delà, défilement interne plutôt que de continuer à grandir). Recalcul déclenché à la saisie et à l'ouverture de l'accordéon. Vérifié en sandbox (contenteditable de test isolé, sans dépendre d'un nouveau déploiement) : contenu court → 110px, contenu long → plafonné à 75vh avec défilement interne disponible au-delà. Commit `94bc6a6`.
- [x] **Correction de `WEB_APP_URL` manquant dans `_test/L2` et `_test/L3` (08/09/2026)** : Fred a testé l'éditeur de texte riche ci-dessus et obtenu `Erreur de chargement : SyntaxError: The string did not match the expected pattern` dès l'ouverture de la page. Cause réelle, sans rapport avec l'éditeur de texte riche : ces deux copies utilisaient encore le placeholder `REMPLACER_PAR_URL_EXEC_TEST` jamais renseigné pour `WEB_APP_URL`, au lieu de la vraie URL `/exec` (la même que PROD/DEV — un seul script Apps Script partagé, seul le paramètre `cible` change) ; `fetch()` avec ce placeholder échoue immédiatement. Bug préexistant, resté invisible jusqu'ici car TEST-L2 n'avait été vérifié que par appel direct à l'URL `/exec`, jamais via la page `index.html` elle-même. Corrigé (commit `993b606`). Après le push de Fred, l'erreur a persisté un instant à cause du cache navigateur (`max-age=600` sur la page GitHub Pages) — résolu par un rechargement forcé / navigation privée.
- [x] **Éditeur de texte riche pour le plan de séance, en test (08/09/2026)**, sur demande de Fred : dans les copies `_test/L2` et `_test/L3` uniquement, le textarea brut du plan de séance est remplacé par une zone `contenteditable` avec une petite barre d'outils (Gras / Italique / Liste à puces / Lien). Le HTML produit est nettoyé côté client avant l'envoi (allowlist de balises, attributs retirés sauf `href` http(s) sur les liens, liens ouverts dans un nouvel onglet) puis stocké tel quel dans la colonne R de Calendrier — aucun changement dans `Code.gs`, qui traite déjà le plan comme une simple chaîne.
- [x] **Neutralisation de `DEV-L2` et `DEV-L3` (08/09/2026)**, sur demande de Fred : plutôt que de dupliquer aussi les classeurs L2/L3 pour un vrai environnement DEV isolé, choix du correctif minimal — Fred testera désormais via TEST-L2 (et TEST-L3 une fois configuré) plutôt que via DEV. `CLASSEURS['DEV-L2']` et `['DEV-L3']` dans `Code.gs` sont remplacés par des placeholders (`REMPLACER_PAR_ID_CLASSEUR_DEV_L2`/`_L3`, même convention que TEST-L3) : tout appel avec `cible=DEV-L2` ou `DEV-L3` échoue désormais proprement (`ouvrirClasseur_` renvoie une erreur explicite) au lieu de lire/écrire silencieusement dans les classeurs PROD-L2/PROD-L3. Script Apps Script redéployé (Version 13 du 8 sept. 2026, 21:30, même URL `/exec`). Vérifié : `?action=data&cible=DEV-L2` et `cible=DEV-L3` renvoient bien une erreur `cible pas encore configurée`, et `?action=data&cible=TEST-L2` continue de fonctionner normalement (non affecté).
- [x] **Configuration du classeur TEST-L2 (08/09/2026)**, pour permettre de tester une évolution sans toucher aux données réelles. Constat de départ : dans `Code.gs`, `CLASSEURS['DEV-L2']` et `['DEV-L3']` pointaient en réalité vers les **mêmes classeurs que PROD-L2/PROD-L3** (pas d'isolement du tout), et `TEST-L2`/`TEST-L3` étaient des placeholders non configurés (`REMPLACER_PAR_ID_CLASSEUR_TEST_*`). Autre risque identifié : `savePresences_` réécrit toutes les présences de la séance à chaque enregistrement — tester sur une séance PROD qui a déjà des présences réelles, sans recocher les mêmes personnes, les efface. Correctif appliqué pour L2 : classeur « AA - Suivi L2 2026-2027 » dupliqué en un classeur indépendant « AA - Suivi TEST-L2 » (id `1jy8hW_2haO3kolxPwgNnT5qANA1kI91_2Kutvn2FQs8`), renseigné dans `CLASSEURS['TEST-L2']`, script Apps Script redéployé (Version 12 du 8 sept. 2026, même URL `/exec`, aucun changement front nécessaire). Vérifié : l'appel `?action=data&cible=TEST-L2` renvoie bien le roster du classeur de test. L'URL `https://apneeattitude.github.io/AppSuiviAA/_test/L2/` est donc maintenant un vrai environnement de test isolé.
- [x] **Fusion de la branche `test-plan-seance` dans `main` (08/09/2026)** : le commit ajoutant l'accordéon « Plan de séance » (7ee504d) avait été fait sur une branche à part et jamais fusionné — `main`, et donc GitHub Pages (PROD comme DEV/TEST, qui ne sont que des sous-dossiers du même site), en était resté à la version précédente (renommage Zone de confort). Fast-forward propre poussé par Fred (`git push origin test-plan-seance:main`), site reconstruit, accordéon confirmé présent dans le DOM de la page L1 en ligne.
- [x] Vérifier le bon fonctionnement des lignes PROD L1, L3, L4 et LC sur le site GitHub Pages, suite au renommage du déploiement Apps Script et à l'ajout de `cache: 'no-store'` sur les appels front — vérifié le 08/09/2026 en même temps que le déploiement de l'accordéon plan de séance (voir ci-dessous) : L1 charge et répond correctement.
- [x] Ajout de Jean-Charles Quincampoix comme **« invité potentiel »** (non membre) dans les classeurs PROD des autres lignes d'eau — **AA - Suivi L1, L3, L4 et LC 2026-2027** (08/09/2026), sur demande de Fred. Même geste que ci-dessus : une ligne ajoutée dans l'onglet Personnes de chaque classeur (ID P104 / QUINCAMPOIX / Jean-Charles / élève / Groupe(s)=L2 — son vrai groupe, non modifié), sans toucher aux colonnes calculées. Comme son Groupe(s) ne contient pas le code de la ligne du classeur (ex. pas "/L1/" dans le classeur L1), la colonne H (Membre de la ligne) reste vide : il n'est pas compté comme membre de ces lignes-là. Mais il obtient quand même un rang global (colonne O, calculé sur tout l'onglet Personnes), ce qui le rend sélectionnable dans le menu déroulant de saisie des présences de chacune de ces lignes — exactement le comportement "invité potentiel" voulu, sans aucune formule ni mécanisme supplémentaire à ajouter. **Point de vigilance** : cette colonne Groupe(s) est en bleu pâle (copiée automatiquement depuis le classeur de Paramétrage à chaque Synchro) — si `synchroniserEffectifs` est lancé un jour sur ces classeurs, vérifier que la ligne de Jean-Charles n'est pas écrasée ou dupliquée (le script de synchro n'a pas encore été conçu/testé pour ce cas d'un apnéiste présent "en invité" dans un classeur qui n'est pas celui de son groupe principal).
- [x] Ajout de **Jean-Charles Quincampoix** (nouvel apnéiste L2, absent du référentiel club Craft.me et du fichier de paramétrage) : créé dans l'onglet Personnes (P104) et inscrit en L2 / élève dans l'onglet Inscriptions du fichier de paramétrage (07/09/2026), **puis ajouté à la main le 08/09/2026 dans le classeur PROD « AA - Suivi L2 2026-2027 »** (une seule ligne ajoutée dans l'onglet Personnes de ce classeur — ID/Nom/Prénom/Rôle déclaré/Groupe(s) ; les colonnes Code, Membre de la ligne, Séances suivies/tenues, Taux de participation et Code élève se sont recalculées seules par formule). Bonne surprise au passage : les onglets Suivi_Objectifs, Suivi_Competences, Avis_Encadrants et la liste Selection (qui alimente le menu déroulant de saisie des présences) sont eux aussi entièrement pilotés par formule depuis l'onglet Personnes (INDEX/MATCH sur une colonne « Code élève », avec des lignes vides pré-préparées jusqu'à 100 apnéistes) : Jean-Charles y est apparu automatiquement, sans aucune autre saisie. **Pour un ajout ponctuel d'une seule personne dans un classeur PROD, inutile donc de lancer `synchroniserEffectifs` ou `sync_referentiel.py` — une simple ligne dans l'onglet Personnes du classeur concerné suffit.** Les scripts de synchro restent utiles pour des mises à jour en masse (plusieurs personnes, plusieurs classeurs).
- [x] Analyser les écarts entre les membres L2 consignés dans le fichier de paramétrage (onglet Inscriptions) et la composition du groupe décrite dans le document de référence du club (https://sides-carry-3kj.craft.me/j2Gng2vJkRJtVa) : un seul écart trouvé — **MADE Jérôme**, présent dans le référentiel Personnes (P018) et dans la liste Craft.me du groupe L2, mais absent de l'onglet Inscriptions pour L2 (07/09/2026). À ajouter dans Inscriptions si Fred confirme.
- [x] Renommage du champ « Qualité » en « Zone de confort » (valeurs : Dans la zone / En limite / Hors zone), suite à la remarque d'un encadrant sur le caractère normatif de « conforme » — mis à jour dans les 7 front-ends de ligne (L1, L2, L3, L4, LC, DNF, STA + copies _test/_dev L2/L3) et dans `generateurs/aa_common.py` (`QUALITE` → `ZONE_CONFORT`), `build_suivi.py`, `build_parametrage.py` (07/09/2026)
- [x] Conversion d'`AA - Parametrage 2026-2027.xlsx` en Google Sheet natif, à la racine de Mon Drive (fait par Fred le 06/09/2026) : https://docs.google.com/spreadsheets/d/18vMX5fqFgCN7NrSkPsSr1ftVoonPFC5xEbjf73iM8Lw
- [x] **Pousser la branche `test-l2-classeur` vers `main`** — fait à ce moment-là, et redevenu une habitude de routine depuis (chaque livraison PROD de cette session se termine par ce même `git push origin test-l2-classeur:main`).
- [x] (partiel) Portage en Apps Script (`Code.gs`, fonction `synchroniserEffectifs`) de la logique déjà écrite et testée hors ligne dans `generateurs/sync_referentiel.py` : synchronise l'onglet Personnes (Nom/Prénom/Rôle déclaré/Groupe(s)/Objectif de la saison) de chaque classeur PROD (L1, L2, L3, L4, LC) depuis le Google Sheet de paramétrage — sans ressaisie. Ne touche jamais aux colonnes A (Code) et H (Membre), qui restent des formules propres à chaque classeur.

## Chantier — Séparer l'application, les modèles et les données du club

Objectif : rendre l'application réutilisable pour une saison ou un club donné, sans versionner dans Git les données opérationnelles du club.

- Créer des modèles neutres et versionnés dans Git : un classeur de paramétrage et un classeur de suivi, sans personnes, inscriptions ni séances réelles.
- Conserver la Google Sheet `AA - Parametrage 2026-2027`, dans le dossier Drive `AppSuivi`, comme source de données métier du club pour la saison. Elle ne doit pas être suivie par Git.
- Versionner dans Git les générateurs Python, les modèles neutres et la documentation ; ignorer les classeurs de production, les exports et les données réelles du club.
- Adapter les générateurs pour qu'ils produisent les classeurs d'un club et d'une saison à partir du modèle et de la source de données indiqués, sans année ou club inscrits en dur.
- Vérifier les fichiers aujourd'hui suivis par Git, sortir progressivement les classeurs de données du suivi Git sans supprimer les fichiers locaux, puis compléter le fichier `.gitignore`.
- Préserver dans le modèle les tables de référence normalisées, notamment `Zone de confort` et `Statuts de séance` avec les colonnes `ID`, `libellé` et `Actif`.
- [x] **Premier lot de ménage Git (20/09/2026)** : les 7 scripts `generateurs/*.py` et `build_all.sh` sont désormais suivis par Git, ainsi que trois documents de proposition non encore versionnés (`doc/prompt-design-bouton-remplacer.md`, `doc/proposition-listes-de-valeurs.md`, `doc/propositions-bouton-remplacer.md`). `.gitignore` complété pour exclure `classeurs/` (arborescence de données réelles attendue par les anciens générateurs), `generateurs/personnes.json` (données nominatives réelles du club, ~80 entrées), les caches Python (`__pycache__/`, `*.pyc`), la config d'éditeur locale (`*.code-workspace`) et les artefacts de debug (`*.inspect.ndjson`) — sans rien supprimer de ces fichiers en local. Au passage, découverte et correction d'une régression locale sans lien avec la prod : les classeurs modèles `L1/`, `L2/`, `L3/`, `L4/`, `LC/`, `DNF/`, `STA/` (`*.xlsx`) étaient vidés de tout leur contenu depuis le 06/09/2026 (0 octet de chaînes texte, contre ~24 Ko dans la version suivie par Git) — artefact ancien et oublié, jamais commité, sans impact sur l'appli (ces fichiers ne sont que des modèles pour réimport dans Google Sheets, jamais lus par le backend en production) ; restaurés à leur dernière version saine par `git checkout`. Suppression confirmée par Fred de deux fichiers obsolètes déjà absents du disque (`indexold.html`, ancien index remplacé par l'appli URL unique ; `_dev/L3/index.html`, lié au workflow DEV abandonné) et de trois fichiers non suivis sans valeur (`AA - Parametrage 2026-2027 - listes normalisees.xlsx` et son `.inspect.ndjson` de 3,7 Mo, `DNF1/AA - Suivi TEST-DNF1 2026-2027.xlsx`, `DNF2/AA - Suivi TEST-DNF2 2026-2027.xlsx`). Reste à faire pour ce chantier : classeurs modèles neutres, adaptation des générateurs à l'arborescence actuelle par ligne (`L1/`, `L2/`, …) plutôt que `generateurs/`+`classeurs/`.

## Fonctionnalités à venir

Idées de fonctionnalités futures, classées par priorité (P1 = prioritaire, P3 = à explorer sans urgence) :

- [ ] Définir et implémenter des règles d'affectation des encadrants lorsqu'une même séance peut compter plusieurs encadrants potentiels : présence simultanée, alternance, remplacement et information affichée dans l'application. En attendant, conserver la règle simple d'un **encadrant référent** par créneau, ligne et date d'effet ; il alimente l'encadrant habituel et le remplacement.
- [x] **P1 — DNF1, DNF2, STA1 et STA2, corrigé le 23/09/2026 :** la colonne « Membre de la ligne » (onglet Personnes) était figée par une formule posée à la génération du classeur, qui vérifie si le code de la ligne apparaît littéralement dans Groupe(s) — correct pour L1-LC (inscription directe), mais toujours vide pour ces 4 lignes puisque personne n'est jamais inscrit littéralement sous ces codes : leur appartenance dépend des règles d'accès (`Regles_Acces_Seances`), pas d'une inscription directe. Deux options posées à Fred (formule figée régénérée vs calcul par la synchro) — **retenue : calcul par la synchro**, car les règles d'accès sont justement faites pour changer en cours de saison. `lireParametrage_` lit désormais aussi `Regles_Acces_Seances` (onglet séparé) et la section « Groupes de niveaux » de `Referentiels` (repérée via le lien du sommaire, jamais une position figée — nouvelle fonction `trouverPremiereLigneSection_`, même principe que `table_rows()` côté Python). Nouvelle fonction `groupesAutorisesLigne_` (port de `groupes_autorises()`, sync_referentiel.py). `synchroniserPersonnesLigne_` écrase désormais la colonne H pour ces 4 lignes seulement (jamais pour L1-LC ni STAC, dont la formule d'origine reste correcte — STAC a un effectif nominatif via Inscriptions, pas par règle). Diagnostic en lecture seule (`diagnostiquerMembreLigneSeances`) confirmé avant écriture : DNF1→L1/L2/L3/L4, DNF2→L4, STA1→L1/L2, STA2→L3/L4, exactement les groupes attendus. Testé sur les 4 classeurs TEST puis les 4 classeurs PROD (`synchroniserEffectifs`), vérifié via l'API : mêmes effectifs des deux côtés (DNF1 95/97, DNF2 31/97, STA1 36/97, STA2 65/97).
- [ ] **P1 — Modèle des lignes de séances :** retenir comme identifiants de lignes `DNF1`, `DNF2`, `STA1`, `STA2` et `STAC`. DNF1, DNF2, STA1 et STA2 sont définies par des règles d'accès ; STAC (Compétition) est définie par une liste nominative de personnes. Les libellés DNF et STA servent seulement à rendre le nom de ligne lisible et ne constituent pas des catégories métier indépendantes ni des rattachements de personnes.
- [ ] **P1 — Prévoir l'affichage des séances sur une fenêtre glissante :** 30 jours dans le passé et 7 jours dans le futur.
- [ ] **P1 — Ergonomie de la page unique et des statistiques :**
  - refaire le sélecteur de séance : le libellé ne doit plus être tronqué et l'ouverture de la liste doit être placée de façon cohérente, sans entrer en concurrence avec le statut ;
  - remplacer tout message technique `Error` par un message français compréhensible ;
  - pour l'accès refusé aux statistiques générales, afficher un message neutre sans citer de personne ;
  - masquer dans le menu les statistiques auxquelles le compte connecté n'a pas droit, plutôt que de les afficher puis de signaler un refus ;
  - corriger le cadrage de l'écran de statistiques sur smartphone, actuellement positionné en bas à gauche.

  **Lot 1 traité le 20/09/2026 (messages Error + accès refusé), en TEST, validation restant à faire :** cause commune trouvée pour les messages techniques `Error` — `doGet`/`doPost` (`Code.gs`) renvoyaient `String(err)`, qui préfixe tout message d'erreur par `Error: ` (y compris les messages déjà en français) ; corrigé pour renvoyer `err.message`. Les deux points de `_test/index.html` et un de `_test/statistiques/index.html` qui concaténaient une exception JS brute (ex. erreur réseau) dans le message affiché ont été remplacés par un texte français fixe. Le message d'accès refusé aux statistiques générales (`Code.gs`) et le texte d'accueil de la page de connexion (`_test/statistiques/index.html`) ne citent plus Frédéric nommément (« accès réservé aux responsables du club »). Déployé sur le déploiement Apps Script **TEST** et vérifié par appel direct à l'API (20/09/2026) : `action=data&cible=TEST-L2` répond `ok:true`, et un cas d'erreur volontaire confirme l'absence du préfixe `Error:`.

  **Sélecteur de séance / statut, premier correctif le 20/09/2026, sur TEST uniquement :** sur `_test/index.html`, le statut de séance était positionné sur une ligne séparée sous le sélecteur (bloc `.status-row`), ce que Fred ne souhaite pas. Remplacé par une disposition côte à côte sur une seule ligne (nouveau bloc `.session-row`, en flexbox) : le sélecteur de séance (`.session-picker`) prend l'espace disponible (`flex:1`), la pastille de statut (`.status-pill-wrap`) garde sa largeur naturelle à droite, sans chevauchement. Règles ajoutées dans `assets/appsuivi.css` (feuille commune) ; la page PROD (`index.html`) utilise encore l'ancienne disposition (pastille superposée en incrustation dans le sélecteur, `.status-pill-wrap` en position absolue à l'intérieur de `.session-picker`) — à harmoniser avec le nouveau design une fois validé sur TEST.

  **Menu et popup de statistiques, repositionnés le 20/09/2026, sur TEST uniquement :** deux options proposées à Fred sous forme de prototype HTML interactif (comparaison de patterns inspirés de Material Design 3, faute de connecteur Figma autorisé dans la session), choix validés et implémentés :
  - **Menu → tiroir latéral.** `.menu-dialog` n'est plus un panneau compact plaqué en haut de l'écran (`margin: 0 14px 0 auto`) mais un panneau plein hauteur ancré au bord droit (`position:fixed; inset:0 0 0 auto; width:min(82%,300px)`), identique à toutes les tailles d'écran (retiré du bloc de mise en bas de page mobile partagé avec les autres dialogues).
  - **Statistiques de la ligne → dialogue adaptatif.** `.stats-dialog` reste centré par défaut (comportement natif de `<dialog>`, inchangé). Sur petit écran (`max-width:600px`), nouvelle règle dédiée avec un `inset` explicite (`inset: auto 0 0 0; margin:0; width:100%`) au lieu de l'ancien réglage `margin: auto 0 0` partagé avec les deux autres dialogues — probable cause du bug « cadré en bas à gauche » signalé par Fred, un mélange de marges automatiques ambigu selon les navigateurs. Une petite poignée décorative (`.stats-handle`) apparaît en haut du dialogue uniquement dans ce mode feuille-du-bas.

  **Retouches du texte et de l'espacement du tiroir, le 20/09/2026 :** sur demande de Fred, les libellés de ligne perdent le mot « Apnée » et prennent une majuscule initiale (`DNF1` : « Apnée dynamique sans palme débutants » → « Dynamique sans palme débutants » ; même principe pour DNF2, STA1, STA2, STAC). Ce raccourcissement corrige au passage le défaut d'alignement à droite du libellé signalé sur DNF1/DNF2 dans le tiroir (300 px de large) : le texte trop long débordait/repassait à la ligne et cassait l'alignement porté par `margin-left:auto`. Marges resserrées dans `.menu-dialog` (22px 18px → 20px 14px), entre les blocs `.menu-section` (22px → 16px de marge haute) et dans chaque ligne `.menu-line`/`.menu-link` (12px → 8px de marge horizontale), pour redonner de la place au texte dans un tiroir plus étroit que l'ancien panneau (480 px). Icône ♙ devant le nom de l'encadrant retirée (« ça prend de la place pour rien »).

  **Découverte et correctif important le 20/09/2026 : fuite TEST → PROD via `assets/appsuivi.css`.** Ce fichier est chargé par `_test/index.html` **et** `index.html` (PROD), qui partagent en grande partie les mêmes classes (`menu-dialog`, `stats-dialog`, `encadrant-info`, etc.). Les trois chantiers ci-dessus (tiroir, dialogue adaptatif, libellés/marges, icône encadrant) avaient donc été appliqués à PROD aussi, sans validation de Fred sur PROD, avant d'être repérés par vérification directe du CSS servi en ligne. Corrigé par un mécanisme de scoping : `_test/index.html` porte désormais `<body class="env-test">`, et chaque règle spécifique à ces chantiers est préfixée `body.env-test …` dans `assets/appsuivi.css` (bloc commenté « TEST uniquement »), avec la règle de base d'origine restaurée pour PROD. PROD est donc revenu à son état d'avant ces quatre chantiers, en attendant validation de Fred sur TEST puis promotion explicite (même principe que `Code.gs` TEST → PROD). **Pour toute évolution visuelle future scopée à TEST : toujours passer par `body.env-test …` dans `assets/appsuivi.css`, jamais modifier une règle de base partagée sans réfléchir à son effet sur PROD.**

  **Masquage du menu selon les droits, le 20/09/2026, sur TEST uniquement :** les entrées « Statistiques de la ligne » et « Statistiques générales » sont désormais masquées (attribut `hidden`, posé par défaut dans le HTML) plutôt qu'affichées avec un refus après clic. Nouvelle action `autorisations` (POST, `Code.gs`) : vérifie le jeton, puis renvoie deux booléens (`statsLigne`, `statsGenerales`) sans calculer les statistiques elles-mêmes (rapide, pas d'ouverture du classeur de la ligne). La logique d'autorisation par ligne, déjà écrite dans `getStatsLigne_`, a été extraite dans une fonction partagée `autoriseStatsLigne_` pour ne pas la dupliquer. Le front (`_test/index.html`) appelle cette action à chaque changement de ligne (`appliquerLigne_`), puisque le droit sur « Statistiques de la ligne » dépend de la ligne affichée ; le titre de section « Suivi » est masqué aussi si aucune des deux entrées n'est visible. En cas d'échec réseau, le menu reste masqué par défaut (sûr par construction, pas de fuite d'affichage). Nécessite un nouveau déploiement Apps Script **TEST** (copié-collé manuel par Fred, browser automation indisponible dans cette session).

  **✅ Anomalie du 21/09/2026, résolue :** le menu ne masquait pas « Statistiques de la ligne » malgré un attribut `hidden` correctement posé par le JavaScript (confirmé par inspection du DOM avec Fred). Cause réelle, sans lien avec la logique serveur (`autoriseStatsLigne_`, qui s'est révélée correcte tout du long) : `.menu-link { display: flex; ... }` dans `assets/appsuivi.css` est une règle d'auteur, qui l'emporte toujours sur la règle `[hidden] { display: none }` du navigateur — même à spécificité CSS égale, l'origine de la règle prime sur son ordre ou sa spécificité. `hidden` restait donc sans le moindre effet visuel sur tout élément portant par ailleurs sa propre règle `display`. Corrigé par l'ajout de `[hidden] { display: none !important; }` dans `assets/appsuivi.css` (règle globale, non scopée `body.env-test` : correction de comportement standard, sans risque pour PROD qui n'utilise pas encore l'attribut `hidden`). Diagnostic mené avec Fred via la console du navigateur (Network/DevTools, plus fiable que l'onglet Exécutions d'Apps Script ou Journaux Cloud — greyed out sans projet GCP standard lié) : la réponse serveur (`{"statsLigne":false,"statsGenerales":true}`) était juste depuis le début. Diagnostics temporaires retirés (`Code.gs` et `_test/index.html`).

  **Responsables du club pilotés par le classeur, le 21/09/2026 :** sur demande de Fred (« il ne faut pas que cela soit en dur dans le programme »), les listes `ACCES_STATS_GLOBALES_TEST`/`ACCES_STATS_GLOBALES_PROD` (codées en dur dans `Code.gs`) sont supprimées. Le statut de responsable est désormais une donnée du classeur de Paramétrage central : nouvelle colonne « Responsable club » (`oui`/vide) dans l'onglet Personnes, lue par `lireParametrage_` (repérée par son en-tête, comme les autres colonnes de ce type dans ce projet — absente sur un classeur pas encore migré, auquel cas personne n'est responsable). Nouveau helper `estResponsableClub_`, utilisé par `getStatsClub_` et `getAutorisations_` à la place des anciennes listes — la distinction TEST/PROD disparaît au passage (un responsable l'est indépendamment de l'environnement consulté, la donnée vit dans le classeur central, commun aux deux). Migration ponctuelle `preparerResponsablesClub` (à lancer une fois depuis l'éditeur Apps Script, menu Exécuter) : crée la colonne si absente et active `oui` pour Frédéric (`flebrigand@gmail.com`) et **Danny Segui** (`d.segui@psm-ffessm.fr`, nouvellement ajouté à la demande de Fred), recherchés par courriel plutôt que par ID Pxxx. Nécessite le même redéploiement Code.gs que ci-dessus, puis l'exécution manuelle de `preparerResponsablesClub` avant de tester.

  **✅ Livré en PROD le 21/09/2026, sur demande de Fred.** Les chantiers ci-dessus (messages d'erreur propres, sélecteur de séance/statut côte à côte, tiroir latéral, dialogue adaptatif des stats, libellés/marges, icône encadrant retirée, masquage du menu selon les droits, responsables du club pilotés par le classeur) sont promus de `_test/index.html` vers `index.html` (PROD) : les règles CSS jusque-là scopées `body.env-test` dans `assets/appsuivi.css` deviennent les règles par défaut (le scoping n'a donc plus de raison d'être pour ces chantiers), et les mêmes changements HTML/JS que sur TEST sont répercutés dans `index.html` (avec `environnement: 'PROD'`). Mêmes nettoyages de messages d'erreur appliqués à `statistiques/index.html` (page PROD des statistiques générales). **Point de vigilance :** tant que le déploiement Apps Script **PROD** n'est pas explicitement promu à la version déjà validée sur TEST (action `autorisations` inexistante sur l'ancienne version PROD), les entrées « Statistiques de la ligne » et « Statistiques générales » resteront masquées pour tout le monde sur PROD (échec silencieux, sûr par construction) — la promotion Apps Script PROD doit suivre ce push de près.

  **✅ Libellé de séance raccourci, livré en PROD le 22/09/2026** (déjà en place sur TEST) : jour abrégé (« mar. » au lieu de « mardi »), date sans l'année, séparateur « · » — `remplirSeances()` dans `index.html`, identique à `_test/index.html`.
  **✅ Page de statistiques générales autonome supprimée le 22/09/2026, sur TEST et PROD** (`_test/statistiques/`, `statistiques/`) : sans objet depuis la popup qui la remplace (ci-dessus). Le point resté ouvert sur son cadrage mobile n'a donc plus lieu d'être — la popup n'a pas ce problème (dialogue adaptatif déjà corrigé, cf. plus haut).
- [ ] **P1 — Vue globale des statistiques :** écran multi-lignes de TEST créé le 19/09/2026 : comparaison de la fréquentation, des séances tenues et des séances à compléter pour L1, L2, L3, L4, LC, DNF1, DNF2, STA1, STA2 et STAC. La comparaison et le détail sont réunis dans un tableau dépliable, avec une flèche à droite et une commande « Tout déplier / Tout replier » ; chaque ligne affiche la moyenne par jour de la semaine et ses séances à compléter avec l'encadrant concerné. La page TEST consulte les classeurs PROD en lecture seule sauf STAC, qui dispose de son propre classeur TEST. L’accès est vérifié côté serveur et réservé à **Frédéric** (`flebrigand@gmail.com`) et, à terme, aux responsables du club explicitement ajoutés à la liste d’autorisation. La page n’expose pas les noms des participants. **Depuis la version Apps Script TEST 59 (19/09/2026), les statistiques d’une ligne dans l’application unique vérifient le courriel du compte Google contre `Personnes!F` du paramétrage central puis le rôle actif `encadrant` ou `prépa encadrant` de cette ligne.** À valider avec un encadrant, puis compléter la liste des responsables et les lignes avant la livraison PROD.
- [ ] **P1 — Statistiques de fréquentation des lignes :** afficher la fréquentation moyenne de chaque ligne depuis le début de la saison, sa moyenne par mois pour visualiser les évolutions, ainsi que le détail moyen par jour de la semaine (lundi, mardi, etc.). **Première version TEST-L2 livrée le 19/09/2026 :** l'écran mobile calcule uniquement les séances passées, au statut `tenue` et comportant au moins une présence ; les séances annulées, planifiées ou sans saisie sont exclues. Les graphiques mensuels et par jour utilisent une échelle commune pour être comparables ; les trois indicateurs de synthèse tiennent sur une ligne et emploient les libellés « Moyenne par séance », « Tenues » et « À compléter ». Il affiche aussi, en bas de l'écran, le nombre et le détail des séances passées à compléter, avec l'encadrant responsable : une séance sans présence, ni statut `tenue`, `annulée` ou `fermée`. Apps Script TEST version 58 et page `_test/L2/` publiés ; validation fonctionnelle puis généralisation aux lignes PROD restantes à réaliser.
- [ ] **P1 — Contrôle de saisie des séances passées :** identifier les séances dont la saisie n'est pas complétée après leur date, puis les regrouper par ligne et par encadrant habituel ou remplaçant. Définir précisément le critère de séance « non saisie » (aucune présence, statut, ou informations attendues manquantes) avant l'implémentation.
- [ ] **P3** — Interface dédiée aux apnéistes (et non plus seulement aux encadrants) pour qu'ils renseignent eux-mêmes leur ressenti de séance et leur forme du jour. Développement distinct de l'existant car public différent (apnéistes plutôt qu'encadrants), mais qui permettrait aussi, potentiellement, de corréler les avis encadrants et les avis apnéistes.
- [ ] **P2 — Courriels :** reconstituer et vérifier les courriels dans le classeur central, puis les reporter de façon fiable dans les classeurs de ligne. Les courriels erronés ayant été supprimés des classeurs, ne pas les reprendre comme source ; définir d'abord le référentiel unique et le contrôle de cohérence associé.
- [ ] **Demandé par Fred le 22/09/2026 : ajouter les encadrants non fixes.** À préciser avant implémentation (à quelle table du paramétrage cela s'ajoute, comment ça se distingue d'un encadrant habituel/référent/remplaçant déjà modélisés).
- [ ] **Demandé par Fred le 22/09/2026 : ajouter les responsables de lignes dans la liste des responsables du club.** Fait côté code le 22/09/2026 : `lireParametrage_` lit désormais `Responsables_Ligne` (le responsable en vigueur d'un groupe, à la date d'effet la plus récente déjà passée), et `estResponsableClub_` accorde l'accès aux statistiques générales à ce responsable comme à un « Responsable club ». Diagnostic `diagnostiquerResponsablesClub` ajouté pour vérifier qui en profite. **Reste à confirmer :** que le déploiement Apps Script PROD a bien été promu en « Nouvelle version » après ce changement (demandé à Fred le 23/09/2026, confirmation non reçue) — sans ça, cette règle n'est active qu'en TEST.
- [x] **Demandé par Fred le 22/09/2026 : nouvelle échelle de Zone de confort, à 5 niveaux, livrée en PROD le 23/09/2026** (remplace l'ancienne à 3 : Confort/Challenge/Limite) :
  1. Routine (bleu) — environ 10 % du travail. Très facile, quasiment sans difficulté.
  2. Maîtrise (vert) — environ 20 %. Quelque chose que l'on maîtrise, mais qui demande quand même de l'engagement.
  3. Performance (jaune) — environ 40 %. Là où l'on travaille majoritairement pour progresser efficacement.
  4. Défi (orange) — environ 30 %. Fortement challengé, la séance devient difficile et on doit parfois adapter.
  5. Panique (rouge) — à éviter. On dépasse ce qui est constructif, ça peut laisser une trace négative importante.

  Migration à prévoir sur le même principe que celle des statuts de séance (`migrerStatutsSeance`) : la Zone de confort est déjà normalisée en ID (1=Confort, 2=Challenge, 3=Limite) dans les classeurs PROD — passer à 5 niveaux touche la liste de référence, la validation de saisie dans `Presences`, et tout historique déjà saisi (ré-échelonnement à décider, pas une simple extension).
  **Correspondance retenue avec Fred (22/09/2026)** pour tout l'historique déjà saisi : Confort→Routine (1), Challenge→Performance (3), Limite→Panique (5). Maîtrise (2) et Défi (4) n'apparaîtront donc que sur les nouvelles saisies.
  **Migration des données faite et vérifiée sur TEST-L2 (22/09/2026)** : `migrerEchelleZoneConfort_(cible)` + `migrerEchelleZoneConfortTestL2()` dans `Code.gs`. Même prudence que pour STAC : vérifie que `ListeZoneConfort` correspond exactement à l'ancienne échelle avant d'écrire, refuse si un libellé de `Presences` ne correspond à aucune des 3 anciennes valeurs, écrit la nouvelle liste dans de **nouvelles colonnes** de l'onglet Listes (jamais sur l'ancien bloc, même principe que `migrerStatutsSeance`) puis repointe `ListeZoneConfort` dessus, réécrit le libellé de chaque présence historique concernée, et vérifie après coup que l'ID recalculé correspond. Sur TEST-L2 : 4 présences réécrites (2 Challenge→Performance, 2 Confort→Routine), vérifiées via l'API (`listesZoneConfort` renvoie bien les 5 nouvelles valeurs).
  **Front : aucun changement nécessaire au fond** — `_test/index.html` construit déjà son menu déroulant dynamiquement depuis `zonesConfort` (l'API), sans libellé codé en dur ; les 5 nouvelles valeurs s'affichent donc automatiquement.
  **Couleurs, palette choisie avec Fred sur prototype interactif (22-23/09/2026)** : faute d'accès Figma dans cette session, comparaison sur une page HTML cliquable (même procédé que pour le tiroir de menu, cf. plus haut). Palette retenue : « zones d'effort » (inspirée des zones d'entraînement cardio) — 1 Routine bleu `#3d8fd1`, 2 Maîtrise vert `#3ea86b`, 3 Performance jaune clair `#f3dd47` (texte blanc malgré un contraste faible, choix assumé par Fred), 4 Défi orange `#df8a3a`, 5 Panique rouge `#cc4b40`.
  **Composant refait (23/09/2026)** : le `<select>` natif ne peut pas être coloré une fois ouvert sur mobile (habillage du système) — remplacé dans `_test/index.html` par une puce pleine couleur (`construireChipZoneConfort_`) qui ouvre une liste déroulante personnalisée, chaque option marquée d'un point de la couleur correspondante. Toujours entièrement piloté par `zonesConfort` (l'API), aucun libellé codé en dur ; seule la palette (classes `zone-1` à `zone-5`) vit dans `assets/appsuivi.css`, scopée `body.env-test` — les classeurs PROD n'ayant pas encore la nouvelle échelle, une couleur non scopée y afficherait un mauvais code couleur (ex. id 3 = Limite sur PROD, Performance sur TEST). À valider par Fred sur TEST-L2 (rendu ET interaction : ouverture/fermeture de la liste, sélection), puis étendre la migration de données aux autres classeurs (mêmes garde-fous que `migrerEchelleZoneConfort_`) et lever le scoping CSS avant promotion PROD.
  **✅ Livré en PROD le 23/09/2026.** Migration des données sur les 10 classeurs PROD (`migrerEchelleZoneConfortToutesLignesProd`, un `diagnostiquerZoneConfortToutesLignesProd` en amont a confirmé un état identique sur les 10 — pas de surprise façon STAC cette fois). Composant puce + liste et palette portés à l'identique dans `index.html`, scoping `body.env-test` levé dans `assets/appsuivi.css` (règles désormais partagées). Au passage : suppression de Sébastien Dussault des Inscriptions répercutée sur les 10 classeurs PROD via `synchroniserEffectifs`.
- [x] **P2** — Publier le guide d'utilisation AppSuiviAA dans le groupe WhatsApp du club. Le PDF a été mis à jour le 19/09/2026 pour l'application à URL unique de production, son menu et son sélecteur de séance ; il ne mentionne pas la page de statistiques générales.
  **Mis à jour le 21/09/2026** (`guides/build_guide.py`, régénère `guides/Guide-AppSuiviAA.pdf`) : l'étape 1 (page 1) précise qu'on arrive désormais directement sur la ligne qu'on encadre, le menu servant à en choisir une autre. La carte « Participant invité » (page 2, cas plus rare) est remplacée par une carte « Statistiques de la ligne », qui explique que cette entrée du menu est réservée aux encadrants de la ligne et sert à suivre sa fréquentation — sur demande explicite de Fred, sans mentionner les statistiques générales (réservées aux responsables du club, hors périmètre de ce guide). Le caractère ☰ utilisé dans un premier temps ne s'affichait pas avec la police du PDF (rendu en carré noir, Helvetica ne porte pas ce glyphe) ; remplacé par une description en mots (« le menu en haut à droite »), cohérent avec le reste du guide qui ne l'utilisait déjà pas.
  **Passage au format HTML le 23/09/2026, sur demande de Fred** : nouveau `guides/guide.html`, même contenu que le PDF (repris à l'identique), section Zone de confort mise à jour pour l'échelle à 5 niveaux avec un point de couleur par niveau. Le lien « Guide d'utilisation » du menu (`index.html` et `_test/index.html`) pointe désormais vers ce fichier plutôt que vers le PDF. `guides/Guide-AppSuiviAA.pdf` et `guides/build_guide.py` restent dans le dépôt (non maintenus depuis, plus liés depuis l'appli).
- [x] **Bug constaté le 21/09/2026, corrigé et vérifié le 22/09/2026 :** les statistiques L1 (et générales) affichent des « séances à compléter » antérieures au 01/10/2026, alors que la vraie saison L1 ne commence qu'à cette date — le calendrier généré par `sync_referentiel.py` n'a jamais connu de date de début propre à L1, contrairement aux lignes de séances DNF/STA. Fred a proposé, plutôt qu'un correctif ponctuel, de fusionner `Lignes_Seances` dans la section « Groupes de niveaux » de l'onglet Referentiels (ajout d'une colonne Membre = nommé/règle, pour distinguer un groupe de niveau d'une ligne de séances), afin que **toutes** les lignes du club aient une date de début/fin d'effet paramétrable au même endroit.
  **Point 1 — générateurs Python, fait le 21/09/2026 :** `aa_data.py` porte désormais `GROUPES_NIVEAUX` (11 lignes : L1-LC + DNF1/DNF2/STA1/STA2/STA3/STAC, avec Discipline/Rang/Groupe précédent/Membre/Dans le MVP/Nombre de lignes/Début-Fin d'effet/Actif/Commentaire) ; `GROUPES` et `LIGNES_SEANCES` restent disponibles mais sont désormais des vues dérivées (filtrées sur Discipline=DYN, resp. ≠DYN) pour ne rien casser des nombreux autres usages (Objectifs_Niveaux, Competences_Niveaux, Inscriptions, Responsables_Ligne, Regles_Acces_Seances, CODES_AFFECTATIONS). `build_parametrage.py` : la section « Groupes de niveaux » passe de 7 à 12 colonnes et se remplit désormais depuis `GROUPES_NIVEAUX` ; l'onglet `Lignes_Seances` (généré vide jusqu'ici) est supprimé. `sync_referentiel.py` lisait la section « Familles de compétences » de Referentiels à une position de ligne figée (`P_CMP`, fragile — un commentaire du code avertissait déjà qu'elle dépendait de l'ordre des sections) : remplacé par une lecture robuste via la Table Excel nommée (`table_rows()`, à partir de `ws.tables[...].ref`), et la lecture de `lignes_seances` pointe maintenant sur la section fusionnée (colonne Discipline ≠ DYN distingue une ligne de séances). `check_classeurs.py` mis à jour de la même façon (table nommée plutôt que ligne figée). Testé de bout en bout : régénération du paramétrage, `sync_referentiel.py` sur les classeurs locaux (6 lignes de séances bien lues), `check_classeurs.py` (aucune régression, mêmes échecs pré-existants liés à des classeurs vides/non recalculés).
  **Précision du 22/09/2026, à partir du classeur réel de Fred :** la section « Groupes de niveaux » du classeur central portait déjà, avant cette fusion, une colonne **Bassin** (jamais lue par les générateurs) et intégrait déjà DNF1/DNF2/STA1/STA2/STAC comme des lignes à part entière, avec leur propre chaînage Rang/Groupe précédent au sein de leur discipline (ex. STA1→STA2→STAC) — plus riche que ce que les générateurs produisaient. Le schéma Python (`aa_data.py`, `GROUPES_NIVEAUX`) a été corrigé en conséquence : colonne Bassin réintégrée, chaînage Rang/Groupe précédent conservé pour DNF/STA, « Dans le MVP » = non pour ces lignes (comme au niveau de la discipline), dates/capacités reprises telles quelles de l'onglet Lignes_Seances existant. **STA3, qui n'existe encore dans aucun des deux onglets côté classeur réel, a été retiré du générateur** — ce n'était pas un oubli de `LIGNES_SYNC` dans `Code.gs` comme supposé initialement, cette ligne n'a simplement jamais été créée.
  **Reste à faire, côté classeur central en ligne (geste manuel, Fred) :** insérer une colonne Membre juste après Bassin, puis 4 colonnes Nombre de lignes/Début d'effet/Fin d'effet/Actif entre Dans le MVP et Commentaire ; remplir Membre (nommé pour L1-LC et STAC, règle pour DNF1/DNF2/STA1/STA2) et les 4 nouvelles colonnes pour les lignes DNF/STA/STAC à partir des valeurs actuelles de l'onglet Lignes_Seances ; supprimer cet onglet une fois reporté.
  **Purge Code.gs du Calendrier L1 — écrite le 22/09/2026, pas encore exécutée :** `purgerSeancesAvantDate_(cible, dateLimite)` (+ le raccourci `purgerSeancesL1AvantSaison()`, seuil 01/10/2026) supprime les lignes de Calendrier antérieures à la date de saison, en s'appuyant sur `deleteRows()` — la colonne A (id séance) est une formule `COUNTIF` qui se renumérote seule à la suppression. Vérifie d'abord qu'aucune présence n'est déjà enregistrée dans le classeur (sinon la renumérotation désynchroniserait ces présences du texte figé dans Presences) et que les séances à purger sont contiguës depuis le début du calendrier ; sinon, erreur explicite et aucune écriture. À exécuter par Fred depuis l'éditeur Apps Script (menu Exécuter sur `purgerSeancesL1AvantSaison`, résultat visible dans le journal d'exécution) — ne nécessite pas de nouveau déploiement, juste un collage à jour de `Code.gs`.
- [x] **Statistiques générales : affichées comme une popup sans se reloguer, sur TEST le 21/09/2026, livrées en PROD le 22/09/2026.** Le menu PROD pointait encore vers l'ancienne page séparée `statistiques/` (sa propre connexion Google, d'où une nouvelle fenêtre d'authentification signalée par Fred) : porté à l'identique de TEST — bouton de menu, `#statsClubDialog`, `ouvrirStatistiquesClub_`/`afficherStatistiquesClub_`/`creerDetailLigneClub_`, écouteurs d'événements (`environnement: 'PROD'`). Les classes CSS du tableau comparatif étaient déjà dans la feuille partagée `assets/appsuivi.css`, aucun ajout nécessaire. Aucun changement serveur : `getStatsClub_` compare toujours les classeurs PROD réels quel que soit l'environnement appelant. Jusqu'ici, cette vue vivait sur une page séparée (`statistiques/index.html`) avec sa propre connexion Google — d'où une reconnexion à chaque fois et une sortie du menu principal, sans moyen d'y revenir. `_test/index.html` porte désormais un second dialogue (`#statsClubDialog`), ouvert par un bouton du menu (au lieu d'un lien `<a href="statistiques/">`) et alimenté par la même action `statsClub` déjà existante côté `Code.gs` (aucun changement serveur nécessaire) — avec le jeton déjà obtenu à la connexion, sans reconnexion. Le rendu (synthèse + tableau comparatif des lignes, dépliable, « Tout déplier/replier ») reprend et adapte la logique de l'ancienne page, avec les mêmes classes CSS que les statistiques de ligne (`stats-summary`, `stats-kpi`, `stats-row`/`stats-track`/`stats-bar` via `ajouterBarresStats_`, `stats-todo`) plutôt que la palette teal indépendante de l'ancienne page — cohérence visuelle avec le reste de l'appli (Océan profond). Nouvelles classes CSS ajoutées pour le tableau comparatif (`stats-comparison*`, `stats-line-detail`, `stats-line-body`) dans `assets/appsuivi.css`. L'ancienne page `_test/statistiques/index.html` reste en place (accessible directement par son URL) mais n'est plus liée depuis le menu. À valider par Fred puis livrer en PROD (même geste que les chantiers précédents — aucun redéploiement Apps Script nécessaire cette fois, seul le front change).
- [x] **Bug PROD-STAC corrigé le 22/09/2026 : « ID du statut non calculé » en changeant le statut d'une séance.** Diagnostic (`diagnostiquerStatutIdStac`, lecture seule) : PROD-STAC porte bien l'en-tête « Statut (ID) » et la plage nommée `ListeStatutsSeance` (`migrerStatutsSeance` la croit donc déjà migrée et ne touche à rien), mais la formule qui calcule l'ID depuis le libellé manquait précisément sur les toutes premières séances (S001 et suivantes, les seules réellement utilisées) — remplacée par une valeur figée à 1, jamais recalculée. Cause non identifiée avec certitude (vraisemblablement une saisie manuelle antérieure à l'existence de ce mécanisme). Réparation ciblée (`repererFormuleStatutId_` + `repererStatutIdStac`) : n'installe la formule que sur les lignes qui en sont réellement dépourvues (96 lignes sur ce classeur), vérifie après coup que chaque libellé produit bien l'ID attendu, sinon échoue sans rien laisser d'incohérent. Vérifié via l'API : S001/S002 (annulée) affichent désormais `statut_id: 3`, conforme.
- [x] **Ligne par défaut = celle dont on est encadrant, validé sur TEST le 21/09/2026 :** sur demande de Fred, quand l'URL ne porte pas de `?ligne=…`, l'appli interroge d'abord le serveur pour retrouver une ligne dont le compte connecté est encadrant, plutôt que de retomber directement sur L2. Nouvelle action `ligneParDefaut` (POST, `Code.gs`) : parcourt `LIGNES_SYNC` (liste déjà utilisée par la synchronisation des effectifs) et renvoie la première ligne où `autoriseStatsLigne_` est vraie pour ce compte, ou `null` si aucune. `ligneDepuisUrl_` (`_test/index.html`) renvoie désormais `null` en l'absence de paramètre (au lieu de `'L2'` par défaut) ; `handleCredentialResponse` n'appelle `ligneParDefaut` que dans ce cas, et retombe sur `L2` si le serveur ne trouve rien, si la ligne suggérée n'est pas configurée sur cet environnement, ou en cas d'échec réseau. Validé via un log temporaire (retiré ensuite) : le serveur suggère bien `L3` pour le compte de Fred, qui retombe sur L2 dans l'appli faute de `TEST-L3` configuré — comportement attendu, pas une anomalie. **✅ Livré en PROD le 21/09/2026** : mêmes changements appliqués à `index.html` (`ligneDepuisUrl_` renvoie `null` sans paramètre, `handleCredentialResponse` appelle `ligneParDefaut` le cas échéant). PROD-L3 étant réellement configuré, ce sera le premier test grandeur nature du mécanisme pour Fred.
- [x] **P1 — Créer la ligne `STAC` (Compétition) :** le paramétrage central est renseigné le 20/09/2026 : créneau hebdomadaire mardi 21h30–22h30, bassin 25 m (`CR-MAR-25`), actif du 07/09/2026 au 30/06/2027 ; Gérald Lelarge (`P050`) est l'encadrant référent. La composition est nominative, enregistrée dans `Inscriptions` et non déduite de règles d'accès : Viktor Sikora (`P099`), Mohammed Belhadj Kacem (`P101`), Francis Wang (`P013`), Sebastien Aubert (`P100`) et Gérald Lelarge (`P050`). Les Google Sheets natifs TEST et PROD, leurs 43 séances, leurs cibles Apps Script et la page AppSuiviAA sont créés. Les versions Apps Script TEST 63 et PROD 64 incluent leurs synchronisations ciblées, qui rendent aussi tous les encadrants actifs disponibles pour les remplacements. La livraison PROD est effectuée ; contrôler le parcours réel STAC après propagation de GitHub Pages.
- [x] **P2 — Rôles :** rôle `prépa encadrant` attribué dans le référentiel central le 19/09/2026 à Francis Wang (`P013`, L4), Corinne Le Brigand (`P052`, L2) et Magali Cavatore (`P069`, L3), tout en conservant leur rôle `élève`. Testé puis promu en version Apps Script 51 ; les neuf classeurs PROD ont été synchronisés et contrôlés.
- [x] **P2 — Rôles :** rôle `prépa encadrant` ajouté le 19/09/2026. Il est proposé dans le référentiel central et possède le même fonctionnement applicatif que `encadrant`, notamment pour les remplacements.
- [x] **P1 — Cohérence des personnes :** contrôle réalisé le 19/09/2026 : les neuf classeurs PROD (L1, L2, L3, L4, LC, DNF1, DNF2, STA1 et STA2) exposent les 98 personnes actives du référentiel central. Christophe Chapot (`P105`) est présent partout ; Sandry Wallon (`P036`) y porte les rôles `élève` et `encadrant`.
- [x] **P1 — Remplacements DNF1, DNF2, STA1 et STA2 :** la synchronisation inclut désormais ces lignes de séances. Les quatre classeurs TEST ont été contrôlés, puis les quatre classeurs PROD synchronisés le 19/09/2026 avec `synchroniserEffectifsLignesSeancesProd`, après promotion Apps Script TEST puis PROD en version 50. Chaque application expose les 25 encadrants actifs du club comme remplaçants, y compris Dominique Beauvallet (`P030`) qui n'appartient pas aux lignes DNF2 et STA2. STAC sera ajoutée à ce mécanisme lors de sa création.
- [x] **P1** — Afficher et modifier l'état d'une séance depuis l'application. La donnée source est l'onglet `Calendrier`, colonne I (`Statut`). **Implémenté et testé en TEST-L2 le 13/09/2026, puis généralisé et livré en PROD le 13/09/2026** : la Google Sheet centrale `AA - Parametrage 2026-2027` possède les tables de référence normalisées `Zone de confort` et `Statuts de séance` (`ID`, `Libellé`, `Actif`). TEST-L2 et les cinq classeurs PROD configurés (L1, L2, L3, L4, LC) possèdent désormais leur plage `ListeStatutsSeance` (1=planifiée, 2=tenue, 3=annulée, 4=fermée), une colonne `Statut (ID)` en fin de `Calendrier`, et une validation « depuis une plage » sur la colonne I. L'application enregistre l'ID avec le bouton unique « Enregistrer la séance ». **Ergonomie variante A** : pastille compacte et cliquable à droite du libellé « Séance », colorée discrètement selon le statut, généralisée aux 7 pages de ligne. **Règle métier :** après un enregistrement contenant au moins une présence, si la date de la séance est strictement antérieure à la date du jour et si son statut est différent de `tenue`, le serveur le passe automatiquement à `tenue` et l'interface le signale. Une séance du jour ou future n'est pas modifiée. Apps Script PROD mis à jour sur le déploiement existant (version 43) ; pages GitHub publiées sur `main` (commit `181e2c1`). DNF/STA restent hors migration car leurs classeurs PROD ne sont pas encore configurés.
- [x] **P2** — Remplacer le champ d'observation sur une ligne par une zone de texte auto-redimensionnée : hauteur initiale compacte, agrandissement automatique jusqu'à **3 lignes maximum**, puis défilement interne au-delà. Implémenté et publié en TEST-L2, validé par Fred, puis déployé en PROD sur L1/L2/L3/L4/LC (12/09/2026, commit `9aae99d`). Le contenu existant est redimensionné au chargement et le comportement reste adapté au mobile. Le guide PDF a été actualisé avec cette saisie et le bouton de remplacement.
- [x] **P1** — Saisie du remplaçant depuis l'appli, avec sélection parmi tous les encadrants actifs du club (12/09/2026). La fenêtre de remplacement conserve la ligne fixe permettant de revenir à l'encadrant habituel, recherche dans le roster synchronisé et enregistre l'ID du remplaçant dans `Calendrier`. La source des rôles est l'onglet `Inscriptions` du classeur `AA - Parametrage 2026-2027`, reportée dans l'onglet `Personnes` de chaque classeur de ligne. Les personnes cumulant les rôles élève et encadrant sont éligibles. Validé sur TEST-L2 puis déployé en PROD : Apps Script version 40, pages L1/L2/L3/L4/LC publiées sur GitHub.
- [x] **P1** — Saisie de la description de la séance (texte et/ou lien) depuis l'appli, ergonomie retenue : accordéon repliable « 📋 Plan de séance » sous le sélecteur de séance (option B), replié par défaut, prérempli si une valeur existe déjà, coché « ✓ » quand rempli. Enregistré avec le même bouton « Enregistrer la séance » que les présences (pas de bouton séparé). Implémenté le 07/09/2026 : `Code.gs` (lecture/écriture colonne R de Calendrier, invalidation du cache « data ») + les 7 `index.html` de ligne + copies _test/_dev L2/L3. **Déployé en PROD le 08/09/2026** (fusion de la branche `test-plan-seance` dans `main`, confirmé visible en ligne) — reste à tester en conditions réelles par Fred, maintenant possible sans risque via l'environnement TEST-L2 ci-dessus.
- [x] **P2** — Vérifier les participants rattachés à la ligne 4 : préparer et envoyer un message WhatsApp aux encadrants de L4 afin de confirmer la composition actuelle du groupe, puis mettre à jour le paramétrage si nécessaire.

## Refonte du module Suivi — cahier des charges v2

Arbitrages nécessaires avant rédaction (issus de l'analyse du classeur "Suivi Apnéistes L3") :

- [ ] Reprise de l'historique 2025-2026 (525 cellules à éclater, en partie manuellement) ou démarrage à neuf en septembre 2026 avec seulement synthèse + référentiel ?
- [ ] Avis d'encadrants (couleurs `T1`/`T1+T2`) : repris comme données historiques ou reconstitués dès la première campagne dans le nouvel outil ?
- [ ] Échelle souhaitée pour la qualité de réalisation, en remplacement de `ok/x/-` (binaire, trois niveaux, ou l'échelle à 4 niveaux déjà utilisée pour les techniques) ?
- [ ] Objectif "en maîtrise" : les 3 répétitions doivent-elles être faites dans une même séance, ou cumulées sur la saison ?
- [ ] Périmètre de visibilité des commentaires : un encadrant voit-il les commentaires des autres encadrants sur un apnéiste ? Et l'apnéiste lui-même ?
- [ ] Registre dédié aux événements de sécurité, avec alerte en cas de cumul d'incidents pour un même apnéiste ?
- [ ] Les invités deviennent-ils des personnes en base, ou restent une simple mention textuelle sur la séance ? — *note du 08/09/2026 : premier cas concret traité pour Jean-Charles Quincampoix (voir plus haut), en le créant comme personne en base (non-membre) dans les classeurs des autres lignes. Reste à décider si c'est la règle générale pour tout futur invité, ou un cas particulier lié au fait qu'il a par ailleurs un compte L2 réel.*
- [ ] Référentiels d'objectifs/compétences par ligne (L1 à L4, groupe compétition) : existent-ils déjà rédigés au-delà de L3 ?
- [ ] Conserve-t-on la logique de deux campagnes statistiques annuelles (décembre, avril), à quelles dates de référence ?
- [ ] Seuils de fréquentation (vert/jaune/orange/rouge) par ligne d'eau : quelles valeurs retenir ?
